# New26InterThing 项目约定

## 项目目标

本项目用于将 `H:\Project\26InterThing` 分阶段重构为比赛现场易于手动维护的 TypeScript 项目。旧项目是行为参考，比赛需求文档和已确认的安全规则优先；不要原样搬运明显缺陷或无引用代码。

## 已确认架构

- Monorepo 使用 pnpm workspace。
- `apps/web`：Nuxt 4、Vue 3、TypeScript、Element Plus、Tailwind CSS、Pinia、ECharts。
- `apps/server`：Express + TypeScript，负责 HTTP、MySQL、MQTT、WebSocket 和全部控制决策。
- `packages/shared`：只存放前后端确实共用的接口类型。
- 按业务模块纵向组织代码，再在模块内按页面、路由、服务、仓储和类型分工。
- Windows 直接运行前后端；MySQL 使用本机实例，MQTT Broker 使用现有 Docker 容器。应用本身不要求 Docker。

## 不可破坏的兼容边界

- 数据库结构以 `H:\Project\_localhost-2026_09_10_19_38_43-dump.sql` 为准，不修改既有表结构。
- `contest_admin` 是独立后台，只通过同一 MySQL 数据库与本项目协作，不调用本项目 HTTP API。
- MQTT Topic 和设备上下行报文必须兼容旧项目。
- 新前后端 HTTP API 可以重新整理，但已有业务含义不能遗漏。

## 实现原则

- 后端是自动控制和安全保护的唯一决策源；前端只展示、编辑配置并发送人工操作意图。
- 优先级：安全保护/紧急停机 > 人工停机 > 人工临时控制 > 自动控制。
- 不开发登录、权限、移动端、Docker 化、文件日志系统或复杂通用配置系统。
- UI 只适配 `1920×1080` 与 `1366×768` 桌面端，不使用图片素材。
- 前端响应拦截器必须以 JSON 对象打印 HTTP 方法、URL、请求头、参数、状态码和响应。
- 新项目操作日志使用 `t_operation_history`；旧项目的 `t_direct_history` 保持不变。
- 业务命名用清晰英文；只为业务原因、协议与安全规则写必要中文注释。
- 类型、对象、函数参数和较长表达式应使用清晰的多行格式，优先保证比赛现场可读性，不把多个字段或处理步骤压在同一行。
- 新增或修改后端接口时同步维护接口文档和共享类型。
- 不清楚外部接口枚举、字段或协议时停止实现并向用户确认，不猜测。

## 导航规划

- 监控中心：实时监控（首页）、历史数据、行为数据、故障信息。
- 设备与控制：设备管理、指令控制、操作日志。
- 行为数据用于展示智能判定结果。赛方协议统一在 `recognition.adapter.ts` 适配，未取得接口文档前不猜字段。

## 测试与版本控制

- PID、水循环、安全保护和故障判断必须有单元测试；数据库、MQTT、WebSocket 和 HTTP 在公开边界测试。
- PID、水循环、安全保护和故障判断必须有单元测试；普通展示组件不堆叠无业务价值的测试。
- 每个阶段运行相关测试、完整测试和类型检查；按用户要求不运行生产构建，全部开发完成后由用户统一构建。
- 使用 Git 和 Conventional Commits；提交说明使用中文。

## 当前状态

- 主体功能已完成，比赛现场资料只保留在 `docs/` 根目录；开发过程里程碑不再作为当前依据。
- 设备不主动上报业务故障，本地规则统一调用 `reportFault()`；故障页面使用 HTTP 查询，WebSocket 负责实时告警。
- 行为数据由历史记录触发现场 HTTP 智能识别后动态映射入库；赛方协议未明确时不得猜测字段。
- 单设备控制状态使用 `t_direct_global`；只有 `pump`、`heater` 发布设备运行指令，`master` 和其他参数供后端状态机使用。
- 自动水循环、时间比例 PID、累计水量、安全保护、设备在线判断和水力联合诊断均已接入实时链路。
- 告警配置已完成；`t_fault_rule_config` 已同步到 `26InterThing` 与 `teamInterThing`，全部规则均可按比赛要求整体停用，故障记录和右上角弹窗可独立控制，所有修改写入操作历史。
- 实时运行指标清零会同时清空泵/加热时长、一分钟平均流量窗口和出口温度变化窗口，但不清零累计水量；设备传感器值 `>= 600` 统一视为 `0xFFFF` 断线哨兵值。
- 指令页面控制树会消费 `automation.status` 实时状态：使用 `enabled/desiredPump/desiredHeater` 同步 `master/pump/heater` 三个开关；安全故障后立即显示后端期望的关闭状态，故障锁定期间禁止重新开启自动模式，其他停机操作仍可执行。
- 当水泵或加热的后端期望为 `off`、设备实际反馈仍非 `off` 时，指令页面显示“重新发送关闭”；独立接口只允许补发执行器 `off`，绕过相同值去重并记录操作历史，告警锁定期间仍可使用。
- 实时监控趋势图复用现有实时消息与历史趋势接口，未新增设备协议或数据库结构。
- 侧边栏与全局布局体验优化已完成；已还原为老项目 Element Plus 经典黑底与高亮菜单体系并配备 `@element-plus/icons-vue` 图标，最外层与侧边栏、顶部 Header 锁定视口固定显示，仅右侧内容区（`<el-main>`）独立纵向滚动，杜绝长内容滑动时侧边栏与 Header 被滚走的问题。
