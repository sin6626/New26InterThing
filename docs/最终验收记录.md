# 最终验收记录

[返回 README](../README.md)

## 验收范围

- 日期：2026-09-15
- 代码范围：第一至第十里程碑
- 安全边界：不下发真实开泵、加热、校时或累计量清零指令

## 自动化与环境验证

| 项目 | 方式 | 结果 |
|---|---|---|
| 环境与 MySQL | `pnpm check:env` | 通过，环境配置与真实 MySQL 连接正常 |
| 全量测试 | `pnpm test` | 通过，后端 28 个文件 151 项、前端 2 个文件 6 项 |
| TypeScript | `pnpm typecheck` | 通过，shared、server、web 均无类型错误 |
| 生产构建 | `pnpm build` | 待用户统一执行 |
| Markdown 本地链接 | PowerShell 遍历 `README.md` 与 `docs/**/*.md` | 通过，无缺失的本地文档链接 |

## HTTP 只读联调

| 主链路 | 验收点 | 结果 |
|---|---|---|
| 设备 | `GET /api/devices` | 通过，HTTP 200 |
| 历史数据 | `GET /api/sensor-history` 及 `/options` | 通过，均为 HTTP 200 |
| 故障 | `GET /api/faults`、`/options`、`/statistics` | 通过，均为 HTTP 200 |
| 行为数据 | `GET /api/behaviors` 及 `/options` | 通过，均为 HTTP 200 |
| 控制配置 | `GET /api/controls/:deviceNumber` | 通过，HTTP 200 |
| 操作日志 | `GET /api/operation-logs` 及 `/options` | 通过，均为 HTTP 200 |
| 自动化 | `GET /api/automation/debug-mode`、`/:deviceNumber`、`/:deviceNumber/operational-metrics` | 通过，均为 HTTP 200 |

联调设备编号为 `e46488d793245429`。本轮只调用查询接口，没有触发控制、复位、清零或校时。

## 页面验收

| 页面 | `1366×768` | `1920×1080` | 检查点 |
|---|---|---|---|
| `/` 实时监控 | 通过 | 通过 | 卡片、诊断、三张趋势图、右上角状态 |
| `/history` 历史数据 | 通过 | 通过 | 筛选、分页、表格、图表 |
| `/behaviors` 行为数据 | 通过 | 通过 | 动态列、时间筛选 |
| `/faults` 故障信息 | 通过 | 通过 | 统计、筛选、分页 |
| `/controls` 指令控制 | 通过 | 通过 | 安全状态、运行指标、动态控件 |
| `/operation-logs` 操作日志 | 通过 | 通过 | 筛选、分页、结果标签 |
| `/device` 设备管理 | 通过 | 通过 | 设备列表、筛选、分页 |

页面验收使用本机 Chrome 无头模式逐页截图。两种视口下均能完成渲染，固定侧边栏和顶部栏没有随内容滚动丢失；长表格和控制配置保持在右侧内容区内。

## 人工安全验收

- [ ] 真实水泵开/关与设备回报。
- [ ] 建流成功后的真实加热开/关。
- [ ] 正常模式下空转、堵塞、超温和超压故障。
- [ ] 故障安全恢复后的人工复位。
- [ ] 累计水量清零及操作日志。
- [ ] 设备时间同步。

## 待外部条件

- 赛方智能识别正式接口文档尚未提供；当前适配器和 JSON 配置入口已完成，不猜测字段。
- 生产构建结果由用户执行 `pnpm build` 后回填。
- 上述人工安全验收会向真实设备发布有状态指令，须在现场确认水路、供电和人员安全后执行，本轮文档收口不代替现场验收。
