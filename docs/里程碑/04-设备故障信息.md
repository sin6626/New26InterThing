# 第四里程碑：设备故障信息闭环

[返回重构计划目录](../重构计划.md)

### 目标

迁移设备主动上报的故障信息流程，打通“设备 MQTT 上报 → 中文信息映射 → 现有故障表入库 → WebSocket 实时推送 → 故障页面查询与即时更新”。本里程碑只忠实处理设备上报，不提前实现水循环安全判断、自动停机或智能判定。

### 兼容契约

- MQTT 继续使用本机 Docker 中的 EMQX，订阅旧协议主题 `device/error`。
- 上报字段沿用 `d_no`、`e_no`、`type`、可选 `e_msg`、可选 `c_time` 或 `time`。
- 故障记录继续写入 `t_error_msg`，中文映射继续读取 `t_error_code_mapper`；不创建表、不修改字段或索引。
- `type` 作为设备协议原值保存和筛选，不在代码中猜测完整枚举；页面优先显示映射表或记录中的中文信息，未知值保留原始类型码。
- 只有数据库写入成功后才发送 WebSocket 消息，防止页面显示数据库中不存在的故障。

### 后端交付

1. 新建独立 `modules/fault`，集中放置故障消息解析、中文映射、仓储和 HTTP 路由。
2. 扩展现有 MQTT 基础设施，使其同时订阅 `device/sensor` 和 `device/error`，按 Topic 分派给对应模块，避免把两类数据处理混在一个函数中。
3. 校验上报 JSON 与必填的 `d_no`、`e_no`、`type`；时间缺失时使用服务端时间。非法消息只拒绝当前数据，不影响后续 MQTT 消息。
4. 使用 `e_no + type` 查询 `t_error_code_mapper`：命中时使用映射中文信息；未命中时保留设备传入的 `e_msg`；两者都没有时生成包含原始编号和类型的明确文本。
5. 成功写入 `t_error_msg` 后广播共享消息 `fault.realtime`。
6. 提供页面专用接口：
   - `GET /api/faults/options`：返回设备与数据库中已有类型筛选项。
   - `GET /api/faults`：按设备、类型、开始时间、结束时间分页查询，按 `c_time`、`id` 倒序。
   - `GET /api/faults/statistics`：按与列表相同的筛选条件统计各类型数量，供页面图表使用。
7. 参数错误返回 HTTP 400，仓储异常交由统一错误处理中间件返回 HTTP 500。

### 共享类型与消息

- `FaultItem`：记录编号、设备编号、故障编号、原始类型码、中文信息和发生时间。
- `PaginatedFaults`：故障列表分页结果。
- `FaultStatisticsItem`：类型码、显示名称和数量。
- `FaultRealtimeMessage`：`{ type: 'fault.realtime', data: FaultItem }`。

前端只使用共享类型中的业务字段，不直接接触数据库列名；WebSocket 不兼容旧项目的 `error_realtime` 包装格式。

### 前端交付

1. 启用侧栏“故障信息”，路由使用 `/faults`。
2. 在 `features/fault` 中集中维护 API、WebSocket 消息处理、筛选、分页、加载与错误状态，页面只组合 UI。
3. 使用 Element Plus 提供设备、类型、时间范围筛选及分页表格，展示设备编号、故障编号、类型、故障信息和发生时间。
4. 使用 ECharts 展示当前筛选条件下的故障类型数量分布；统计使用独立接口，不根据当前分页的十几条数据计算，避免比例失真。
5. 收到 `fault.realtime` 后：若消息符合当前筛选条件，则刷新第一页列表与统计；不直接把消息硬插入当前页，避免分页总数、顺序和筛选结果不一致。
6. 查询失败时清空旧列表与旧统计并显示明确错误；无数据时显示空状态。
7. HTTP 请求继续由现有响应拦截器打印结构化调用信息。

### 测试 seam

- 解析 seam：合法消息、非法 JSON、缺失必填字段和时间回退规则。
- 映射 seam：映射命中、使用设备 `e_msg` 回退、最终默认文本三种结果。
- 处理 seam：入库成功才广播，入库失败不广播。
- 仓储 seam：列表与统计共用设备、类型和时间条件，分页与排序正确。
- HTTP seam：三个接口返回统一响应，非法参数返回 400，仓储异常返回 500。
- 前端执行 TypeScript 检查和真实浏览器流程验证；按用户约定，阶段开发期间不执行生产构建。

### 验收场景

1. 故障页首次打开能读取真实 `t_error_msg`，筛选、分页和类型统计正常。
2. 向 `device/error` 发布一条合法旧协议消息后，记录只写入一次，页面无需手动刷新即可看到更新结果。
3. 仅提供 `e_no + type` 时能使用 `t_error_code_mapper` 得到中文信息；未知映射仍能保存可理解的回退文本。
4. 设备、类型和时间筛选下，列表总数与统计使用相同条件。
5. 非法 JSON、缺失必填字段和数据库写入失败不会广播虚假数据，也不会使服务退出。
6. 在 `1366×768` 与 `1920×1080` 下页面无横向溢出。
7. 自动化测试与 `pnpm typecheck` 通过；生产构建留到全部里程碑开发完成后由用户统一执行。

### 明确不做

- 不实现水循环状态机、安全保护、故障诊断或自动下发停机指令。
- 不接入智能判定，不写入行为数据。
- 不开发确认、处理、删除、批量操作或声音告警。
- 不维护错误码映射表；该表继续由现有项目管理后台管理。
- 不迁移旧页面中写死的 1 至 6 类型名称，除非数据库映射或设备协议能够明确提供含义。
- 不实现设备心跳或在线/离线判断。
